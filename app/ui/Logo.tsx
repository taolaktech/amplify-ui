import LogoIcon from "@/public/logo-white.svg";

export default function Logo() {
  return (
    <div className="flex items-center justify-center gradient rounded-lg h-[32px] w-[32px]">
      <LogoIcon width={22} height={16} />
    </div>
  );
}
